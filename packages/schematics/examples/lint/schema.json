{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsExamplesLintSchema",
  "title": "Schematic Options Schema",
  "type": "object",
  "properties": {
    "tslint": {
      "type": "string",
      "description": "The path to tslint.json. By default uses the one in the current directory",
      "default": "./tslint.json"
    }
  },
  "required": [
  ]
}
